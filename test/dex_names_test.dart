import 'package:test/test.dart';
import 'package:squidrouter/squidrouter.dart';

// tests for DexNames
void main() {
  final instance = DexNamesBuilder();
  // TODO add properties to the builder and call build()

  group(DexNames, () {
  });
}
